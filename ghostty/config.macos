# macOS-specific
macos-option-as-alt = true

# Theme (set directly on macOS — no theme switcher)
theme = Everforest Dark Medium

# Copy/paste with Cmd+C/V (defaults on macOS, explicit for clarity)
keybind = cmd+c=copy_to_clipboard
keybind = cmd+v=paste_from_clipboard
